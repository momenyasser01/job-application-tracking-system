# Normalize to LF in the repo so a Windows checkout never produces CRLF diffs
# that fail `pnpm format:check` in CI.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
